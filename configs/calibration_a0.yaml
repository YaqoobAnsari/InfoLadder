# Phase A0 — instrument calibration on synthetic planted targets (plan §6; ROADMAP A0-2)
# PASS criterion: recovered saturation level per planted target; controls at ~zero.
# This config is immutable once a registered run has used it (results/CLAUDE.md).
experiment: calibration_a0
seed: 20260714
device: cpu

n_buildings: 60
levels: [0, 1, 2, 3, 4]
targets:
  - planted_degree
  - planted_tau
  - planted_delta
  - planted_zone
  - planted_tau_ctrl
  - planted_zone_ctrl
families: [V1, V2, V3, V4, V5]   # V0 auto-added where a readout exists (zone@R4)
seeds: [0, 1, 2]
n_restarts: 3
mdl: true

margin_nats: 0.1
ctrl_tol_nats: 0.05
